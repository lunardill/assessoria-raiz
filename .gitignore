# Variáveis de ambiente (API keys, tokens — NUNCA commitar)
.env
.env.local
.env.*

# Arquivo gerado pelo /setup com MCPs específicos do usuário
_configurar-mcps.md

# Outputs de renderização (binários pesados)
*.png

# Arquivos de dados do usuário (CSVs, planilhas, etc)
dados/*.csv
dados/*.xlsx
dados/*.xls
dados/*.txt
dados/*.pdf
dados/*.json
!dados/README.md

# Vídeos e áudios brutos (gravações de reunião, etc) — grandes demais pro GitHub
dados/*.mov
dados/*.MOV
dados/*.mp4
dados/*.MP4
dados/*.mkv
dados/*.MKV
dados/*.avi
dados/*.AVI
dados/*.mp3
dados/*.MP3
dados/*.wav
dados/*.WAV
dados/*.m4a
dados/*.M4A

# Sistema
.DS_Store
node_modules/
